AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Official Document Generator API

Parameters:
  Stage:
    Type: String
    Default: test
    AllowedValues: [test]
  GitHubOwner:
    Type: String
    Default: ryucc
  GitHubRepo:
    Type: String
    Default: OfficialDocumentGenerator
  GitHubBranch:
    Type: String
    Default: main
  CodeStarConnectionArn:
    Type: String
    Description: ARN of the CodeStar connection to GitHub

Globals:
  Function:
    Runtime: java21
    MemorySize: 512
    Timeout: 30

Resources:

  # ---- API ----
  DocumentInstructionFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub document-instruction-${Stage}
      Handler: com.officialpapers.api.handler.DocumentInstructionHandler
      CodeUri: ../build/distributions/lambda.zip
      Events:
        ListInstructions:
          Type: Api
          Properties:
            Path: /api/v1/document-instructions
            Method: GET
        CreateInstruction:
          Type: Api
          Properties:
            Path: /api/v1/document-instructions
            Method: POST
        GetInstruction:
          Type: Api
          Properties:
            Path: /api/v1/document-instructions/{instructionId}
            Method: GET
        UpdateInstruction:
          Type: Api
          Properties:
            Path: /api/v1/document-instructions/{instructionId}
            Method: PUT
        DeleteInstruction:
          Type: Api
          Properties:
            Path: /api/v1/document-instructions/{instructionId}
            Method: DELETE

  # ---- Email Filter (runs first to block non-allowlisted senders) ----
  EmailFilterFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub email-filter-${Stage}
      Handler: com.officialpapers.api.handler.EmailFilterHandler
      CodeUri: ../build/distributions/lambda.zip
      MemorySize: 256
      Timeout: 10
      Environment:
        Variables:
          ALLOWED_SENDERS: shortyliu@gmail.com

  EmailFilterPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt EmailFilterFunction.Arn
      Action: lambda:InvokeFunction
      Principal: ses.amazonaws.com
      SourceAccount: !Ref AWS::AccountId

  # ---- Email Processor ----
  EmailProcessorFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub email-processor-${Stage}
      Handler: com.officialpapers.api.handler.EmailProcessorHandler
      CodeUri: ../build/distributions/lambda.zip
      MemorySize: 1024
      Timeout: 300
      Environment:
        Variables:
          ALLOWED_SENDERS: shortyliu@gmail.com
      Policies:
        - S3ReadPolicy:
            BucketName: !Ref DocumentBucket
        - DynamoDBCrudPolicy:
            TableName: !Ref DocumentInstructionTable
      Events:
        EmailQueue:
          Type: SQS
          Properties:
            Queue: !GetAtt IncomingEmailQueue.Arn
            BatchSize: 1

  # ---- DynamoDB (Test) ----
  DocumentInstructionTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub document-instructions-${Stage}
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: id
          AttributeType: S
      KeySchema:
        - AttributeName: id
          KeyType: HASH

  # ---- S3 (Test) ----
  DocumentBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub official-doc-generator-${Stage}-${AWS::AccountId}

  DocumentBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref DocumentBucket
      PolicyDocument:
        Statement:
          - Effect: Allow
            Principal:
              Service: ses.amazonaws.com
            Action:
              - s3:PutObject
            Resource: !Sub ${DocumentBucket.Arn}/emails/*
            Condition:
              StringEquals:
                aws:SourceAccount: !Ref AWS::AccountId

  # ---- SNS Topic for Email Notifications ----
  IncomingEmailTopic:
    Type: AWS::SNS::Topic
    Properties:
      TopicName: !Sub incoming-emails-${Stage}

  IncomingEmailTopicPolicy:
    Type: AWS::SNS::TopicPolicy
    Properties:
      Topics:
        - !Ref IncomingEmailTopic
      PolicyDocument:
        Statement:
          - Effect: Allow
            Principal:
              Service: ses.amazonaws.com
            Action:
              - sns:Publish
            Resource: !Ref IncomingEmailTopic
            Condition:
              StringEquals:
                aws:SourceAccount: !Ref AWS::AccountId

  # ---- SQS Queue subscribed to SNS ----
  IncomingEmailQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub incoming-emails-${Stage}
      VisibilityTimeout: 300
      MessageRetentionPeriod: 1209600  # 14 days

  IncomingEmailQueuePolicy:
    Type: AWS::SQS::QueuePolicy
    Properties:
      Queues:
        - !Ref IncomingEmailQueue
      PolicyDocument:
        Statement:
          - Effect: Allow
            Principal:
              Service: sns.amazonaws.com
            Action:
              - sqs:SendMessage
            Resource: !GetAtt IncomingEmailQueue.Arn
            Condition:
              StringEquals:
                aws:SourceArn: !Ref IncomingEmailTopic

  IncomingEmailQueueSubscription:
    Type: AWS::SNS::Subscription
    Properties:
      Protocol: sqs
      TopicArn: !Ref IncomingEmailTopic
      Endpoint: !GetAtt IncomingEmailQueue.Arn
      RawMessageDelivery: false

  # ---- SES Email Receiving ----
  EmailReceiptRuleSet:
    Type: AWS::SES::ReceiptRuleSet
    Properties:
      RuleSetName: !Sub gongwengpt-ruleset-${Stage}

  EmailReceiptRule:
    Type: AWS::SES::ReceiptRule
    Properties:
      RuleSetName: !Ref EmailReceiptRuleSet
      Rule:
        Name: !Sub forward-to-s3-${Stage}
        Enabled: true
        Recipients:
          - gongwengpt.click
        Actions:
          - LambdaAction:
              FunctionArn: !GetAtt EmailFilterFunction.Arn
              InvocationType: RequestResponse
          - S3Action:
              BucketName: !Ref DocumentBucket
              ObjectKeyPrefix: emails/
              TopicArn: !Ref IncomingEmailTopic

  # ---- CodePipeline ----
  ArtifactBucket:
    Type: AWS::S3::Bucket

  PipelineRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: codepipeline.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: CodePipelineAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:PutObject
                  - s3:GetBucketVersioning
                Resource:
                  - !GetAtt ArtifactBucket.Arn
                  - !Sub ${ArtifactBucket.Arn}/*
              - Effect: Allow
                Action:
                  - codebuild:StartBuild
                  - codebuild:BatchGetBuilds
                Resource: !GetAtt BuildProject.Arn
              - Effect: Allow
                Action:
                  - cloudformation:*
                  - iam:PassRole
                Resource: '*'
              - Effect: Allow
                Action:
                  - codestar-connections:UseConnection
                Resource: !Ref CodeStarConnectionArn

  BuildRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: codebuild.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: CodeBuildAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:PutObject
                Resource:
                  - !GetAtt ArtifactBucket.Arn
                  - !Sub ${ArtifactBucket.Arn}/*
              - Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: '*'

  DeployRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: cloudformation.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AdministratorAccess

  BuildProject:
    Type: AWS::CodeBuild::Project
    Properties:
      Name: !Sub official-doc-generator-build-${Stage}
      ServiceRole: !GetAtt BuildRole.Arn
      Artifacts:
        Type: CODEPIPELINE
      Environment:
        Type: LINUX_CONTAINER
        ComputeType: BUILD_GENERAL1_SMALL
        Image: aws/codebuild/amazonlinux2-x86_64-standard:5.0
      Source:
        Type: CODEPIPELINE
        BuildSpec: infra/buildspec.yaml

  Pipeline:
    Type: AWS::CodePipeline::Pipeline
    Properties:
      Name: !Sub official-doc-generator-${Stage}
      RoleArn: !GetAtt PipelineRole.Arn
      ArtifactStore:
        Type: S3
        Location: !Ref ArtifactBucket
      Stages:
        - Name: Source
          Actions:
            - Name: GitHub
              ActionTypeId:
                Category: Source
                Owner: AWS
                Provider: CodeStarSourceConnection
                Version: '1'
              Configuration:
                ConnectionArn: !Ref CodeStarConnectionArn
                FullRepositoryId: !Sub ${GitHubOwner}/${GitHubRepo}
                BranchName: !Ref GitHubBranch
              OutputArtifacts:
                - Name: SourceOutput
        - Name: Build
          Actions:
            - Name: Build
              ActionTypeId:
                Category: Build
                Owner: AWS
                Provider: CodeBuild
                Version: '1'
              Configuration:
                ProjectName: !Ref BuildProject
              InputArtifacts:
                - Name: SourceOutput
              OutputArtifacts:
                - Name: BuildOutput
        - Name: DeployTest
          Actions:
            - Name: DeployTest
              ActionTypeId:
                Category: Deploy
                Owner: AWS
                Provider: CloudFormation
                Version: '1'
              Configuration:
                ActionMode: CREATE_UPDATE
                StackName: !Sub official-doc-generator-app-${Stage}
                TemplatePath: BuildOutput::packaged-template.yaml
                Capabilities: CAPABILITY_IAM,CAPABILITY_AUTO_EXPAND
                RoleArn: !GetAtt DeployRole.Arn
                ParameterOverrides: !Sub '{"Stage": "${Stage}"}'
              InputArtifacts:
                - Name: BuildOutput

Outputs:
  ApiUrl:
    Description: API Gateway URL
    Value: !Sub https://${ServerlessRestApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/api/v1/document-instructions
  TableName:
    Value: !Ref DocumentInstructionTable
  BucketName:
    Value: !Ref DocumentBucket
  IncomingEmailTopicArn:
    Description: SNS Topic ARN for incoming emails
    Value: !Ref IncomingEmailTopic
  IncomingEmailQueueUrl:
    Description: SQS Queue URL for incoming emails
    Value: !Ref IncomingEmailQueue
  IncomingEmailQueueArn:
    Description: SQS Queue ARN for incoming emails
    Value: !GetAtt IncomingEmailQueue.Arn
  EmailRuleSetName:
    Description: SES Receipt Rule Set Name
    Value: !Ref EmailReceiptRuleSet
