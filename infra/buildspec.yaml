version: 0.2

phases:
  build:
    commands:
      - chmod +x gradlew
      - ./gradlew clean build
      - sam package --template-file infra/template.yaml --s3-bucket $ARTIFACT_BUCKET --output-template-file packaged-template.yaml

artifacts:
  files:
    - packaged-template.yaml
